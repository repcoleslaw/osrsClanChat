# Backend (Render, Railway, Fly.io, local, etc.)
MONGODB_URI=mongodb+srv://andrewrrcole_db_user:nXhPH75gw08WMVRr@osrsclancluster.gq06vr8.mongodb.net/?appName=osrsClanCluster
MONGODB_DB=osrsClanCluster

# Optional: serve ./web from the API on the same port (local dev)
# SERVE_STATIC=1

# Vercel (Project Settings → Environment Variables), no trailing slash
# API_BASE_URL=https://your-api.onrender.com
